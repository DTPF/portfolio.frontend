import { useContext } from "react";
import { MainMenuContext } from "../providers/MainMenuProvider";

// eslint-disable-next-line import/no-anonymous-default-export
export default () => useContext(MainMenuContext);
